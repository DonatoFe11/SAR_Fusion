# Install this overlay only in the dedicated `sarfusion-yolo26` Conda
# environment.  The historical environments must retain the THU-MIG
# Ultralytics 8.1.34 fork required by the completed YOLOv10 experiments.
ultralytics==8.4.138
ultralytics-thop==2.1.6
ultralytics-platform==0.1.21
filelock==3.32.5
pytest==8.3.5

# ABI pair already supplied by the cloned Conda environment and asserted by
# the Stage A runner before training.
torch==2.4.0
torchvision==0.19.0
