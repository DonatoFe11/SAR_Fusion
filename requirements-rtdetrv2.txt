# Install this overlay only in a clone of the historical `sarfusion` Conda
# environment.  It adds the first stable RT-DETRv2 integration used here while
# leaving the environment of the completed RT-DETR experiments untouched.
transformers==4.51.3
tokenizers==0.21.1
huggingface-hub==0.30.2
safetensors==0.5.3
timm==1.0.11
