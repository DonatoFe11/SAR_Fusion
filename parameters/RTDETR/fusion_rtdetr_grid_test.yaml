experiment:
  name: RTDETR_FAM_Evaluation_Grid
  group: RTDETR_Investigation
  continue_with_errors: False
  search: grid

parameters:
  seed: &seed [42]
  task: [detection]

  tracker:
    ignored_files: ["*.bin,*.safetensors"]
    val_image_log_frequency: [50] 
    tags: [[FinalReport]]

  model:
    name: [fusion_rtdetr_fam]
    params:
      id2label:
        0: [person]
      threshold: [0.01, 0.05, 0.1]
      pretrained_path: ["checkpoints/model.safetensors"]

  dataset:
    name: [wisard]
    root: [dataset/WiSARD]
    preprocessor:
      path: ["PekingU/rtdetr_r50vd"]
    folders: [vis, ir, vis_ir] 
    single_class: [true]
    use_tiling: [false]  

  dataloader:
    num_workers: [8]
    batch_size: [12]

  val_evaluation:
    metrics:

other_grids: []