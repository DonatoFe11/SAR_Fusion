experiment:
  name: DINO_DefDETR_FAM_Test1
  group: DINO_Investigation
  continue_with_errors: False
  search: grid

parameters:
  seed: &seed [42]
  task: [detection]

  tracker:
    ignored_files: ["*.bin,*.safetensors"]
    val_image_log_frequency: [50]
    tags: [[FinalReport]]

  model:
    name: [fusion_dino_defdetr]
    params:
      id2label:
        0: [person]
      threshold: [0.01, 0.1]
      pretrained_path: ["checkpoints/dino_defdetr_fam1.safetensors"]
      num_feature_levels: [4]
      use_fam: [true]
  
  dataset:
    name: [wisard]
    root: [dataset/WiSARD]
    preprocessor:
      path: ["SenseTime/deformable-detr"]
    folders: [vis, ir, vis_ir]
    single_class: [true]
    use_tiling: [false]

  dataloader:
    num_workers: [4]
    batch_size: [2]

  val_evaluation:
    metrics:

other_grids: []
