experiment:
  name: DINO_Fusion_DefDETR_baseline_test
  group: DINO_Investigation
  continue_with_errors: False
  search: grid

parameters:
  seed: &seed [42]
  task: [detection]

  tracker:
    ignored_files: ["*.bin,*.safetensors"]
    val_image_log_frequency: [50]
    tags: [[FinalReport]]

  model:
    name: [fusion_dino_defdetr]
    params:
      id2label:
        0: [person]
      threshold: [0.01, 0.05, 0.1]
      pretrained_path: ["checkpoints/model.safetensors"]
      num_feature_levels: [4]
      use_fam: [false]  # IMPORTANT: Must match the checkpoint trained with FAM
  
  dataset:
    name: [wisard]
    root: [dataset/WiSARD]
    preprocessor:
      path: ["SenseTime/deformable-detr"]
    folders: [vis, ir, vis_ir]
    single_class: [true]
    use_tiling: [false]

  dataloader:
    num_workers: [4]
    batch_size: [2]

  val_evaluation:
    metrics:

other_grids: []
